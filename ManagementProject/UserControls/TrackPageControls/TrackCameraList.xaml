<UserControl x:Class="ManagementProject.UserControls.TrackCameraList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ManagementProject.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="300">
    <Grid Background="#FF323232">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#FF0072B7">
            <Label Content="摄像机列表" FontSize="15" Foreground="White"  VerticalContentAlignment="Center" HorizontalAlignment="Left" Width="100"/>
            <Image Source="/ManagementProject;component/ImageSource/Icon/MainWinControlIcon/摄像机白.png" HorizontalAlignment="Right" Width="30" Margin="0,0,10,0" />
        </Grid>

        <StackPanel Grid.Row="1">
            <ListView ItemsSource="{Binding CameraLists}"  HorizontalAlignment="Center" MaxHeight="700" Background="{x:Null}" BorderBrush="{x:Null}" Margin="0 18 0 0" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                            <Border CornerRadius="5" Width="270" Height="45" Background="#FF252525" Margin="5 0">
                                <Border.Effect>
                                     <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="0" Opacity="1" />
                            </Border.Effect>
                                <Grid>
                                    <Label Content="{Binding Name}" FontSize="16" Foreground="White"  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10 0" />
                                    <Grid HorizontalAlignment="Right" Margin="0,0,13,0">
                                        <Ellipse Height="30" Width="30" Fill="#FF464646"  />
                                        <Image  Height="15" VerticalAlignment="Center" HorizontalAlignment="Center" Source="/ManagementProject;component/ImageSource/Icon/MainWinControlIcon/摄像机白.png"/>
                                    </Grid>
                                </Grid>
                            </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid >
                <Button Margin="15,10,0,0" Content="确 定" Command="{Binding GetSelectedCameraCommand}" Background="#FF00A7F4" BorderBrush="{x:Null}" Style="{StaticResource FilletButton}" FontSize="14" HorizontalAlignment="Left" Width="140" Height="38" VerticalAlignment="Top" />
                <Button Margin="0,10,15,0" Content="返 回" Command="{Binding CloseCameraSelectedWinCommand}" Background="#FF00A7F4" BorderBrush="{x:Null}" Style="{StaticResource FilletButton}" FontSize="14" HorizontalAlignment="Right" Width="140" Height="38" VerticalAlignment="Top" />
            </Grid>

        </StackPanel>
    </Grid>
</UserControl>
